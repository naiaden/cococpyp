language: cpp
sudo: false
compiler:
- gcc
install:
  - cd hpyplm && g++ -std=c++0x -DkORDER='3' -g -O3 -I.. $< -o $@ -lcolibricore -lboost_filesystem -lboost_system -I$(COCO_INCLUDE) -lboost_serialization -lbz2
